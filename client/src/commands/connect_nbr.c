#include "zappy_client.h"


// connect_nbr remain <A> used <B> tick <C> lvl <D> (A : slot restant, B : slot utilisé C : tick serveur à vivre, D : niveau actuel)
int		zappy_connect_nbr(zappy_client_t *client)
{
	(void)client;

	int	r = 0;

	fprintf(stderr, "%s\n", __func__);
	return (r);
}
